## Codebase Patterns
- Todas las tablas financieras (transactions, wallets, balance_snapshots, categories) estan particionadas por `perfil` (TEXT). Cualquier query/metodo nuevo en db.py DEBE recibir `perfil` y filtrar por el, nunca leer/escribir sin ese filtro.
- `_ensure_wallets_for_profile(perfil)` se llama al inicio de casi cualquier metodo de billeteras -- crea las 4 billeteras (Bs/BDV, USD/Binance, USD/Efectivo, COP/Efectivo) en 0 si es la primera vez que se ve ese perfil. `is_new_profile(perfil)` debe llamarse ANTES de cualquier metodo que dispare esto, si se necesita saber si es la primera vez.
- Comandos nuevos van en src/bot/commands.py y siguen el patron: `async def X_command(update, context)` -> `if not _is_allowed(update, context): return` -> obtener `db`/`perfil` -> logica -> `await _reply(update, texto)` (usar `_reply` de src/bot/replies.py, no `update.message.reply_text`, asi el comando tambien funciona como boton del /menu si se agrega ahi).
- Registrar todo comando nuevo en src/main.py: import desde `src.bot.commands` (o `src.bot.handlers` si es un handler de mensaje/foto/voz) + `application.add_handler(CommandHandler("nombre", funcion))`.
- Verificacion: `python3 -m py_compile $(find src -name "*.py")` (compile check de todo el arbol) + `.venv/bin/python -c "import src.main"` (smoke test de imports; el `python3` del sistema NO tiene `dotenv` instalado, hay que usar el venv). Para probar logica de db.py sin levantar el bot: `.venv/bin/python -c "from src.storage.db import DBClient; db = DBClient(db_path=':memory:', initial_balance=0, fixed_categories=['Otros']); ..."`.
- Personalidad de Coco: TODO texto fijo dirigido al usuario (comandos, errores) usa "usted" formal, tono de banquero de confianza, emojis minimos (solo los ya establecidos: 🐊 para la voz de Coco, y uno por seccion como maximo). Nunca "tu"/"tú"/"Intenta"/"Usa" informal -- usar "su"/"usted"/"Intente"/"Use". La voz LLM-generada (_persona_coco en prompt_builder.py) ya sigue este registro; los textos hardcodeados deben ser consistentes con ella (unificado en commit 5093de6).
- MONEDA_SIMBOLO y CUENTA_EMOJI (en src/bot/constants.py) son los diccionarios de simbolos/emoji reusables para mensajes de saldo/transferencia.
- **Estructura de src/bot/ (desde el refactor de organizacion de carpetas, ver entrada mas abajo)**: `access.py` (control de acceso + resolucion de perfil), `replies.py` (`_reply`/`_menu_keyboard`), `constants.py` (MONEDA_SIMBOLO/CUENTA_EMOJI), `formatters.py` (arma texto de confirmaciones/tasas, sin tocar Telegram ni DB), `texts.py` (textos largos estaticos: bienvenida, /help), `commands.py` (los `*_command` de Telegram), `handlers.py` (`handle_message`/`handle_photo`/`handle_voice`/`_save_and_confirm`/`error_handler`). El viejo `telegram_handler.py` (883 lineas) ya NO EXISTE -- fue reemplazado por estos 7 archivos. Cualquier import viejo a `src.bot.telegram_handler` esta roto a proposito.
- **Estructura de src/storage/ (mismo refactor)**: `DBClient` (en db.py) es la unica clase publica y su API no cambia nunca por reorganizacion interna. Por dentro compone 5 mixins via herencia multiple: `SchemaMixin` (schema.py, migraciones), `WalletsMixin` (wallets.py, saldo/transfer), `TransactionsMixin` (transactions.py, gasto/ingreso/resumen/racha), `BudgetsMixin` (budgets.py), `FxCacheMixin` (fx_cache.py). `constants.py` tiene MONEDAS_VALIDAS/WALLETS/resolve_cuenta compartidos por los mixins. Si se agrega un metodo nuevo a DBClient: identificar a que dominio pertenece y agregarlo al mixin correspondiente (o crear uno nuevo si es un dominio nuevo), NUNCA agregar logica de dominio directo en db.py (ese archivo solo debe tener `__init__`/`close`/la composicion de mixins).
- Nunca commitear .env ni *.db (ya estan en .gitignore). Nunca imprimir secretos reales en output visible.
- Para mandar un archivo (no solo texto) hay que usar `target.reply_document(...)` con `target = update.message or update.callback_query.message` (mismo patron que `_reply`, pero `_reply` solo soporta texto por ahora -- reply_document se llama aparte).

## [2026-07-02] - US-001
- Implementado /exportar: comando que genera un CSV (con BOM utf-8-sig para que Excel lea bien los acentos) de todas las transacciones (gasto/ingreso) del perfil y lo manda como documento de Telegram.
- Files changed: src/storage/db.py (nuevo metodo `get_all_transactions(perfil)`), src/bot/telegram_handler.py (nuevo `exportar_command`, agregado a los textos de /help y de bienvenida), src/main.py (registrado el CommandHandler).
- **Learnings for future iterations:**
  - `get_all_transactions` NO incluye `balance_snapshots` (ajustes directos de saldo) ni `transfer` (movimientos entre billeteras propias) -- solo gasto/ingreso. Si mas adelante se quiere un respaldo mas completo, habria que unir las 3 tablas.
  - Verificado con DB en memoria (`db_path=':memory:'`) que el aislamiento por perfil funciona (un perfil no ve transacciones de otro).
---

## [2026-07-02] - US-002
- Implementado /deshacer: revierte el ultimo gasto/ingreso registrado por el perfil (revierte el delta en la billetera y borra la fila de `transactions`), y confirma con el nuevo saldo.
- Files changed: src/storage/db.py (nuevo metodo `delete_last_transaction(perfil)`), src/bot/telegram_handler.py (nuevo `deshacer_command`, agregado a /help y bienvenida), src/main.py (registrado el CommandHandler).
- **Learnings for future iterations:**
  - Alcance reducido a proposito: NO deshace ajustes de saldo directos (`set_wallet_balance`, usados por /saldo_inicial y frases tipo "tengo X en efectivo") ni `transfer()` (mueve dinero entre billeteras propias). Ambos dejan registro en `balance_snapshots`, pero un `transfer()` inserta 2 filas con el mismo `created_at` (una por cada billetera) mientras que un ajuste simple inserta 1 -- revertirlos de forma segura requeriria distinguir ese caso y tocar 2 billeteras a la vez. Se dejo fuera para no arriesgar corromper saldos en una primera iteracion; si se pide extender /deshacer a estos casos, ese es el punto de partida.
  - Testeado con DB en memoria: revertir un gasto de $5 en Efectivo deja el balance en 0 (estaba en -5), un segundo /deshacer sin nada que deshacer retorna None (el comando responde con mensaje amigable, no error), y el aislamiento por perfil se mantiene.
---

## [2026-07-02] - US-003
- Implementados presupuestos por categoria: tabla `budgets` (perfil, categoria, limite_mensual, PK compuesta), comando /presupuesto (sin args lista, con "<categoria> <monto>" fija/reemplaza, con monto 0 elimina), y alerta automatica en la confirmacion de gasto (via `_budget_alert_line`) cuando el gasto acumulado del mes en esa categoria llega a 80% (aviso suave) o supera 100% (aviso directo) del limite -- tono banquero, una sola linea, sin emojis de alarma.
- Files changed: src/storage/db.py (tabla `budgets` en `_init_schema`, metodos `set_budget`, `get_all_budgets`, `get_budget_status`), src/bot/telegram_handler.py (`_budget_alert_line`, `presupuesto_command`, `format_confirmation_message` ahora acepta `budget_status` opcional, `_save_and_confirm` calcula el estado del presupuesto SOLO si `tipo == 'gasto' and moneda == 'Bs'` antes de armar el mensaje), src/main.py (registrado el CommandHandler).
- **Learnings for future iterations:**
  - Los limites de presupuesto son SIEMPRE en Bs (moneda del gasto del dia a dia en efectivo/BDV). Un gasto en USD o COP simplemente no dispara ni cuenta contra ningun presupuesto en esta iteracion -- si se quiere soportar multi-moneda en presupuestos habria que decidir una tasa de conversion consistente primero.
  - `get_budget_status(perfil, categoria, fecha)` retorna None si la categoria no tiene presupuesto fijado -- SIEMPRE chequear por None antes de leer 'porcentaje'/'gastado' (asi se evita calcular de mas en el camino feliz, que es el 99% de los gastos sin presupuesto configurado).
  - El calculo del mes usa `substr(fecha, 1, 7) = 'YYYY-MM'` sobre la columna `fecha` (no `created_at`), coherente con como /resumen ya agrupa por mes.
  - `ON CONFLICT(perfil, categoria) DO UPDATE` (upsert) es el patron a usar para tablas con PK compuesta (perfil, algo_mas) que se "fijan"/reemplazan en vez de acumularse -- mismo espiritu que wallets.
  - Testeado con DB en memoria: 420/500 -> 84% (aviso "va en 84%"), 620/500 -> 124% (aviso "ya superó"), categoria sin presupuesto -> None (sin aviso), set_budget con monto 0 elimina la fila, aislamiento por perfil verificado.
---

## [2026-07-02] - US-004
- Agregado aviso de variación relevante (>=3%) en la tasa BCV/Binance al consultar /cambio o /saldo. Se calcula al vuelo: cada vez que el cache de 1h vence y se refresca con un valor nuevo desde la API, se compara contra el valor que HABÍA en cache justo antes de sobrescribirlo. Si el cache sigue vigente (no hubo fetch nuevo), no hay variación que mostrar (no se inventa comparando contra algo mas viejo).
- Files changed: src/services/fx.py (`_get_rate_full` retorna `(rate, previous_or_None)`; `_get_rate` ahora es un wrapper que descarta el previo, sin romper a nadie que ya llamaba `get_bcv_rate`/`get_binance_rate`; nuevas `get_bcv_rate_with_variation`/`get_binance_rate_with_variation`), src/bot/telegram_handler.py (`_rate_variation_pct`, `_variation_note`, `_format_rates_block` ahora acepta `bcv_var`/`binance_var` opcionales; `cambio_command` y `saldo_command` usan las nuevas funciones *_with_variation; `resumen_command` se dejó SIN cambios, usa `get_bcv_rate`/`get_binance_rate` planas a propósito, ver abajo).
- **Learnings for future iterations:**
  - Alcance a propósito: solo /cambio y /saldo muestran variación (son las consultas "¿cómo está la tasa ahora?"). /resumen muestra las tasas solo como referencia de cierre de mes, no tiene sentido ahí y se dejó tal cual para no ensuciar ese mensaje.
  - Para testear esto sin llamar la API real: `db._conn.execute("UPDATE fx_rates SET fetched_at = '2000-01-01T00:00:00' WHERE page=?", (cache_key,))` fuerza que el cache se vea vencido, y luego se puede monkeypatchear `fx._fetch_rate_from_api = lambda page: <valor>` para controlar qué "nueva" tasa llega.
  - El umbral de 3% y el emoji 🔺/🔻 están en `_variation_note` (telegram_handler.py) -- si se quiere ajustar la sensibilidad del aviso, es el único lugar a tocar.
---

## [2026-07-02] - US-005
- Nuevo comando /racha: expone directamente `db.get_current_streak(perfil)` (ya existía, solo se veía dentro de /saldo) con un mensaje corto en la voz de Coco. Maneja el caso 0 (sin racha activa) con un mensaje de aliento en vez de un simple "0".
- Files changed: src/bot/telegram_handler.py (nuevo `racha_command`, agregado a /help y bienvenida), src/main.py (registrado el CommandHandler).
- **Learnings for future iterations:**
  - No hizo falta tocar db.py -- `get_current_streak(perfil, reference_date=None)` ya existía y es reutilizable tal cual.
  - Con esta historia se completó el PRD inicial (US-001 a US-005), todas con `passes: true`. Ver la sección "Codebase Patterns" al inicio de este archivo antes de agregar nuevas historias -- resume las convenciones ya establecidas (aislamiento por perfil, uso de `_reply`, registro en main.py, verificación con py_compile + smoke tests en `.venv/bin/python`).
  - Ideas para una futura ronda (no implementadas, quedan como backlog si se quiere seguir): extender /deshacer a ajustes de saldo y transferencias (ver nota en US-002); soportar presupuestos en USD/COP además de Bs; agrupar /racha y el recordatorio nocturno de main.py para que compartan la misma lógica de "achievement" (ej. avisar hitos como 7/30 días).
---

## [2026-07-02] - Refactor: clean code + organización de carpetas
- Pedido explícito del usuario ("Agrega clean code al codigo y organiza las carpetas"), fuera del flujo de historias del PRD (las 5 ya estaban `passes: true`). Se dividieron los dos archivos monolíticos del proyecto en módulos por responsabilidad, sin cambiar ningún comportamiento observable (mismo texto de mensajes, misma API pública, mismos nombres de comando).
- `src/bot/telegram_handler.py` (883 líneas) -> eliminado, reemplazado por 7 archivos en src/bot/: `access.py` (control de acceso/perfil), `replies.py` (`_reply`/`_menu_keyboard`), `constants.py` (MONEDA_SIMBOLO/CUENTA_EMOJI), `formatters.py` (arma texto de confirmaciones/tasas), `texts.py` (bienvenida/`_help_text`), `commands.py` (los `*_command`), `handlers.py` (`handle_message`/`handle_photo`/`handle_voice`/`_save_and_confirm`/`error_handler`).
- `src/storage/db.py` (725 líneas) -> reducido a 76 líneas (solo `__init__`/`close`/composición), con la lógica repartida en 5 mixins + un módulo de constantes en src/storage/: `constants.py`, `schema.py` (SchemaMixin), `wallets.py` (WalletsMixin), `transactions.py` (TransactionsMixin), `budgets.py` (BudgetsMixin), `fx_cache.py` (FxCacheMixin). `DBClient(SchemaMixin, WalletsMixin, TransactionsMixin, BudgetsMixin, FxCacheMixin)` compone todo -- la API pública (nombres/firmas de método) no cambió, así que ningún call-site (`src/main.py`, `src/bot/commands.py`, `src/bot/handlers.py`) tuvo que tocar la forma en que usa `db.algo(...)`.
- Files changed: eliminado `src/bot/telegram_handler.py`; creados `src/bot/{access,replies,constants,formatters,texts,commands,handlers}.py` y `src/storage/{constants,schema,wallets,transactions,budgets,fx_cache}.py`; reescrito `src/storage/db.py`; actualizado `src/main.py` (imports ahora vienen de `src.bot.commands` y `src.bot.handlers` en vez de `src.bot.telegram_handler`).
- **Learnings for future iterations:**
  - Antes de dividir un archivo grande, grep todo el repo por el nombre del módulo viejo (`grep -rn "telegram_handler"`) para confirmar quién lo importa -- en este caso solo `src/main.py`, lo que hizo seguro borrar el archivo viejo sin dejar imports rotos en otro lado.
  - Patrón de mixins para clases DAO/Repository grandes: cada mixin es una clase normal (sin `__init__` propio) que asume que `self._conn` ya existe: SIEMPRE se combinan en una clase final que sí define `__init__` (aquí, `DBClient`). Nunca instanciar un mixin solo.
  - Al dividir un módulo de comandos de Telegram, cuidado con las dependencias cruzadas: `handlers.py` necesita `menu_command` y `_maybe_welcome_new_profile` de `commands.py` (no al revés) -- se verificó el grafo de dependencias antes de escribir código para evitar imports circulares (access/replies/constants no dependen de nada interno; formatters/texts dependen solo de `src.services.fx`; commands depende de access/replies/formatters/texts/constants; handlers depende de todos los anteriores + commands).
  - Verificación usada (sin servidor real): `python3 -m py_compile $(find src -name "*.py")`, `.venv/bin/python -c "import src.main"`, y un smoke test funcional con `DBClient(db_path=':memory:', ...)` que ejercitó CADA método público (wallets, transacciones, deshacer, presupuestos, resumen, exportar, racha, fx cache, aislamiento por perfil) para confirmar que el comportamiento es idéntico al de antes del refactor.
  - La organización de carpetas a nivel raíz del proyecto (.env, prd.json, progress.txt, run.py, systemd/, etc.) ya era razonable/estándar y no se tocó -- el refactor fue solo dentro de src/bot/ y src/storage/, que eran los dos outliers de tamaño (el resto de los archivos ya estaban bajo 300 líneas).
---
